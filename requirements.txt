# Conduit runtime dependencies.
# Install with:  pip install -r requirements.txt
#
# Everything here is pure-async. There is intentionally no plexapi and no
# qbittorrent-api: both are synchronous and force thread-pool workarounds.
# Conduit talks to Plex, TMDB, the tracker and qBittorrent over httpx directly.

fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic>=2.9
pydantic-settings>=2.6
aiosqlite>=0.20
tomli-w>=1.0

# Optional: Windows system-tray launcher (`conduit run --tray`).
pystray>=0.19
pillow>=10.0

# Optional: development / test tooling.
pytest>=8.3
pytest-asyncio>=0.24
respx>=0.21
ruff>=0.7
